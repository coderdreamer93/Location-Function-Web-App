const path = require('path');

// For React apps using react-scripts, no custom webpack config is needed
// react-scripts handles all webpack configuration automatically
module.exports = {
  // Remove or comment out custom webpack configurations
  // that might conflict with react-scripts
}